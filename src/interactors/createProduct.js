
const createProduct = (ProductDataSource) => async => {
    // validate product data from req.body
    // if invalid return 400 and validation error
    // if valid create product
}

module.exports = createProduct